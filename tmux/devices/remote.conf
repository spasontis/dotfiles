# Generic fallback for any host that's neither WSL nor the m0nstertrack
# desktop (e.g. a remote server reached over SSH). No device-specific
# overrides yet — add them here instead of the shared .tmux.conf.
